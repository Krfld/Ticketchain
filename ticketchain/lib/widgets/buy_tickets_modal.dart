import 'package:flutter/material.dart';

class BuyTicketsModal extends StatelessWidget {
  const BuyTicketsModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
